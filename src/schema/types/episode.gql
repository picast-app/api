type Episode implements Node {
  id: ID!
  title: String!
  description: String
  publishDate: String
  file: String
  duration: Int
}

type EpisodeDiff {
  podcast: ID!
  added: [Episode!]
  removed: [ID!]
}
