type User {
  id: ID!
  authProvider: AuthProvider!
  subscriptions(known: [ID!]): Subscriptions!
  wsAuth: ID
  currentEpisode: EpisodePlayback
}

enum AuthProvider {
  google
}

type Subscriptions {
  added: [Podcast!]!
  removed: [ID!]!
}
